---
phase: 03-fluxo-questionario
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/Loading.jsx
autonomous: true
requirements:
  - FL-03
must_haves:
  truths:
    - "Loading.jsx usa fundo escuro (bg-navy-900 card) coerente com questionnaire escuro"
    - "Mensagens de loading transitam com fade AnimatePresence (sem troca abrupta)"
    - "Spinner usa gold-500 em vez de money-500 (verde)"
    - "Nenhum flash branco ao transitar de questionnaire para loading"
  artifacts:
    - path: frontend/src/components/Loading.jsx
      provides: "Navy+gold redesign + AnimatePresence message fade"
      contains: "AnimatePresence"
---

<objective>
Redesenhar Loading.jsx com visual navy+gold coerente com o questionário (escuro), adicionar
AnimatePresence para fade entre mensagens de loading, e garantir que não há flash branco
na transição questionnaire → loading (o App.jsx já usa bg-slate-950 para ambos após plan 03-01).

Requirement: FL-03 (Submit → loading não causa flash branco).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fluxo-questionario/03-CONTEXT.md
@.planning/PROJECT.md
</context>

<interfaces>
<!-- Current Loading.jsx structure (simplified) -->
<!--
export default function Loading({ businessData, financialData, accessToken, onComplete, onError }) {
  const [messageIndex, setMessageIndex] = useState(0);
  const [error, setError] = useState(null);
  const [countdown, setCountdown] = useState(null);
  // ... fetchDiagnosis logic (DO NOT TOUCH) ...

  if (countdown !== null) { return <div className="card p-8 text-center ..."> ... </div>; }
  if (error) { return <div className="card p-8 text-center ..."> ... </div>; }
  return (
    <div className="card p-10 text-center animate-fade-in">
      <div className="flex justify-center mb-7">
        <div className="relative w-16 h-16">
          <div className="absolute inset-0 rounded-full border-2 border-ink-100" />
          <div className="absolute inset-0 rounded-full border-2 border-transparent border-t-money-500 animate-spin" />
          ...
        </div>
      </div>
      <p className="text-[11px] font-bold text-money-600 uppercase ...">businessName</p>
      <h2 className="text-xl font-bold text-ink-800 ...">LOADING_MESSAGES[messageIndex]</h2>
      ...
      {/* dot indicators */}
    </div>
  );
}
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Loading.jsx — navy+gold visual + AnimatePresence message fade</name>
  <files>frontend/src/components/Loading.jsx</files>
  <read_first>
    - frontend/src/components/Loading.jsx — read full file before editing; preserve ALL fetch/retry logic
  </read_first>
  <action>
    Redesign Loading.jsx visual layer ONLY. Do NOT touch: fetchDiagnosis, useEffect fetch logic,
    autoRetryCount, fetchedRef, supabase call, response streaming, error handling logic.

    Changes:

    1. **Add Framer Motion import** at top (after existing React import):
       ```js
       import { motion, AnimatePresence } from 'framer-motion';
       ```

    2. **Main loading state render** — replace the return JSX (when no error, no countdown) with:
       ```jsx
       return (
         <motion.div
           className="rounded-2xl p-10 text-center border border-white/10"
           style={{ background: '#253d63' }}
           initial={{ opacity: 0, y: 16 }}
           animate={{ opacity: 1, y: 0 }}
           transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
         >
           {/* Gold spinner */}
           <div className="flex justify-center mb-8">
             <div className="relative w-16 h-16">
               <div className="absolute inset-0 rounded-full border-2 border-white/10" />
               <div className="absolute inset-0 rounded-full border-2 border-transparent border-t-gold-400 animate-spin" />
               <div className="absolute inset-0 flex items-center justify-center">
                 <div className="w-4 h-4 rounded-full bg-gold-500/20 flex items-center justify-center">
                   <div className="w-2 h-2 rounded-full bg-gold-500" />
                 </div>
               </div>
             </div>
           </div>

           {/* Business name */}
           <p className="text-[11px] font-bold text-gold-400 uppercase tracking-widest mb-3">
             {businessData.businessName}
           </p>

           {/* Animated message — AnimatePresence fade */}
           <div className="relative min-h-[56px] flex items-center justify-center mb-3">
             <AnimatePresence mode="wait">
               <motion.h2
                 key={messageIndex}
                 className="text-xl font-bold text-white tracking-tight"
                 initial={{ opacity: 0, y: 8 }}
                 animate={{ opacity: 1, y: 0 }}
                 exit={{ opacity: 0, y: -8 }}
                 transition={{ duration: 0.3, ease: 'easeInOut' }}
               >
                 {LOADING_MESSAGES[messageIndex]}
               </motion.h2>
             </AnimatePresence>
           </div>

           <p className="text-white/40 text-sm mb-8">
             Analisando seus números — leva alguns segundos.
           </p>

           {/* Dot indicators */}
           <div className="flex justify-center gap-1.5">
             {LOADING_MESSAGES.map((_, i) => (
               <motion.div
                 key={i}
                 animate={{
                   width: i === messageIndex ? 20 : 6,
                   backgroundColor: i === messageIndex ? '#F59E0B' : 'rgba(255,255,255,0.15)',
                 }}
                 transition={{ duration: 0.3 }}
                 className="h-1 rounded-full"
               />
             ))}
           </div>
         </motion.div>
       );
       ```

    3. **Countdown state render** — replace with blue version:
       ```jsx
       if (countdown !== null) {
         return (
           <motion.div
             className="rounded-2xl p-8 text-center border border-white/10"
             style={{ background: '#253d63' }}
             initial={{ opacity: 0 }}
             animate={{ opacity: 1 }}
           >
             <div className="mx-auto w-12 h-12 rounded-full bg-amber-500/10 border border-amber-500/20 flex items-center justify-center mb-5">
               <svg className="w-5 h-5 text-amber-400 animate-pulse-slow" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.8}>
                 <path strokeLinecap="round" strokeLinejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
               </svg>
             </div>
             <h2 className="text-lg font-bold text-white mb-2">A IA está sobrecarregada</h2>
             <p className="text-white/50 text-sm mb-6 leading-relaxed">
               Muita gente usando agora. Tentando novamente em…
             </p>
             <div className="w-14 h-14 rounded-full border-2 border-white/15 flex items-center justify-center mx-auto mb-6">
               <span className="text-xl font-bold text-white font-mono">{countdown}</span>
             </div>
             <button onClick={() => { setCountdown(null); fetchDiagnosis(); }} className="btn-back">
               Tentar agora
             </button>
           </motion.div>
         );
       }
       ```

    4. **Error state render** — replace with blue version:
       ```jsx
       if (error) {
         const overloaded = isOverloadedMsg(error);
         return (
           <motion.div
             className="rounded-2xl p-8 text-center border border-white/10"
             style={{ background: '#253d63' }}
             initial={{ opacity: 0 }}
             animate={{ opacity: 1 }}
           >
             <div className="mx-auto w-12 h-12 rounded-full bg-red-500/10 border border-red-500/20 flex items-center justify-center mb-5">
               <svg className="w-5 h-5 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.8}>
                 <path strokeLinecap="round" strokeLinejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z" />
               </svg>
             </div>
             <h2 className="text-lg font-bold text-white mb-2">
               {overloaded ? 'IA temporariamente indisponível' : 'Algo deu errado'}
             </h2>
             <p className="text-white/50 text-sm mb-6 leading-relaxed">
               {overloaded ? 'A IA está com alta demanda agora. Aguarde alguns segundos.' : error}
             </p>
             <button onClick={() => { autoRetryCount.current = 0; fetchDiagnosis(); }} className="btn-primary">
               Tentar novamente
             </button>
             <button onClick={onError} className="btn-back mt-2.5">
               Voltar ao formulário
             </button>
           </motion.div>
         );
       }
       ```

    IMPORTANT: Keep ALL logic above the return statements EXACTLY as-is:
    - LOADING_MESSAGES array
    - isOverloadedMsg function
    - MAX_AUTO_RETRIES constant
    - All useState, useRef, useEffect, useCallback
    - fetchDiagnosis function (entire body)
    - The useEffect that calls fetchDiagnosis
  </action>
  <verify>
    <automated>cd "C:/Users/mateu/Desktop/mvp cont/frontend" && node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/Loading.jsx','utf8'); if(!c.includes('AnimatePresence')) throw new Error('AnimatePresence missing'); if(!c.includes('bg-navy-900')) throw new Error('navy bg missing'); if(!c.includes('border-t-gold-400')) throw new Error('gold spinner missing'); console.log('PASS: Loading redesign applied');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AnimatePresence" frontend/src/components/Loading.jsx` returns ≥1 line
    - `grep -n "bg-navy-900" frontend/src/components/Loading.jsx` returns ≥3 lines (main + countdown + error)
    - `grep -n "border-t-gold-400" frontend/src/components/Loading.jsx` returns 1 line
    - `grep -n "fetchDiagnosis" frontend/src/components/Loading.jsx` returns same count as before (logic preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Loading.jsx redesigned dark navy, gold spinner, AnimatePresence message fade. Build passes.</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/mateu/Desktop/mvp cont/frontend"
npm run build
```
Build must exit 0.
</verification>

<success_criteria>
- Loading.jsx: dark navy bg, gold spinner, AnimatePresence fade between messages (FL-03 visual)
- No white flash: App.jsx (from 03-01) uses bg-slate-950 for both questionnaire and loading steps
- All fetch/retry/streaming logic in Loading.jsx untouched
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-fluxo-questionario/03-02-SUMMARY.md` following the summary template.
</output>
