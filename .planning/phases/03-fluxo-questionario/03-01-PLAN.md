---
phase: 03-fluxo-questionario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Questionnaire.jsx
  - frontend/src/App.jsx
autonomous: true
requirements:
  - FL-01
  - FL-02
must_haves:
  truths:
    - "Avançar questão desliza pra esquerda, voltar desliza pra direita"
    - "Barra de progresso anima com spring quando currentIndex muda"
    - "Visual do questionário usa bg-navy-900 cards no wrapper escuro"
  artifacts:
    - path: frontend/src/components/Questionnaire.jsx
      provides: "Slide horizontal AnimatePresence + navy redesign"
      contains: "AnimatePresence"
    - path: frontend/src/App.jsx
      provides: "Dark wrapper bg para questionnaire step"
      contains: "bg-slate-950"
---

<objective>
Redesenhar Questionnaire.jsx com slide horizontal entre perguntas (AnimatePresence direction-aware),
barra de progresso com spring animation, e visual navy+gold coerente com a landing.
App.jsx recebe bg-slate-950 no wrapper quando step === 'questionnaire' para eliminar contraste com Loading (FL-03 preparação).

Requirements: FL-01 (slide), FL-02 (progress), partial FL-03 (dark wrapper prep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fluxo-questionario/03-CONTEXT.md
@.planning/PROJECT.md
</context>

<interfaces>
<!-- Current Questionnaire.jsx key structure -->
<!--
export default function Questionnaire({ onComplete, onBack, initialValues, businessData }) {
  const [currentIndex, setCurrentIndex] = useState(0);
  const [values, setValues] = useState(...);
  const question = QUESTIONS[currentIndex];
  const progress = ((currentIndex + 1) / QUESTIONS.length) * 100;

  function handleNext() {
    if (!canProceed) return;
    if (currentIndex < QUESTIONS.length - 1) {
      setCurrentIndex(i => i + 1);
      window.scrollTo({ top: 0, behavior: 'smooth' });
    } else { onComplete(numericData); }
  }
  function handleBack() {
    if (currentIndex > 0) { setCurrentIndex(i => i - 1); ... }
    else { onBack(); }
  }
  return (
    <div className="animate-slide-up grid lg:grid-cols-[1.1fr_400px] gap-6 max-w-5xl w-full mx-auto">
      <div> {/* top bar, progress, card com pergunta */}
      <div className="hidden lg:block"> {/* LiveDRE */}
    </div>
  );
}
-->

<!-- App.jsx wrapper for non-landing steps -->
<!--
<div className={step === STEPS.LANDING ? '' : 'min-h-screen flex items-start sm:items-center justify-center p-4 py-8 bg-ink-50'}>
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Framer Motion slide transition + navy redesign to Questionnaire.jsx</name>
  <files>frontend/src/components/Questionnaire.jsx</files>
  <read_first>
    - frontend/src/components/Questionnaire.jsx — read full file before editing
    - frontend/src/index.css — confirm available CSS classes (btn-gold, card, etc.)
  </read_first>
  <action>
    Make the following changes to Questionnaire.jsx:

    1. **Imports** — add to existing React import line:
       ```js
       import { motion, AnimatePresence, useSpring, useTransform } from 'framer-motion';
       ```

    2. **Direction ref** — add after existing useState declarations (before `const question = ...`):
       ```js
       const dirRef = useRef(1); // 1=forward, -1=backward
       ```

    3. **Slide variants** — add as module-level constant (before the component, after QUESTIONS array):
       ```js
       const slideVariants = {
         enter: (dir) => ({ x: dir > 0 ? 60 : -60, opacity: 0 }),
         center: { x: 0, opacity: 1, transition: { type: 'spring', stiffness: 280, damping: 26 } },
         exit: (dir) => ({ x: dir > 0 ? -40 : 40, opacity: 0, transition: { duration: 0.18, ease: 'easeIn' } }),
       };
       ```

    4. **Update handleNext** — set dirRef.current = 1 before setCurrentIndex:
       ```js
       function handleNext() {
         if (!canProceed) return;
         if (currentIndex < QUESTIONS.length - 1) {
           dirRef.current = 1;
           setCurrentIndex(i => i + 1);
           window.scrollTo({ top: 0, behavior: 'smooth' });
         } else {
           // existing onComplete logic unchanged
         }
       }
       ```

    5. **Update handleBack** — set dirRef.current = -1 before setCurrentIndex:
       ```js
       function handleBack() {
         if (currentIndex > 0) {
           dirRef.current = -1;
           setCurrentIndex(i => i - 1);
           window.scrollTo({ top: 0, behavior: 'smooth' });
         } else {
           onBack();
         }
       }
       ```

    6. **Animated progress bar** — replace the existing progress bar div with:
       ```jsx
       <div className="mb-7">
         <div className="flex items-center justify-between mb-2">
           <span className="text-xs font-semibold text-gold-400 uppercase tracking-wider">
             {question.chapter}
           </span>
           <span className="text-xs font-semibold text-white/60 font-mono">
             {Math.round(progress)}%
           </span>
         </div>
         <div className="w-full h-1.5 bg-white/10 rounded-full overflow-hidden">
           <motion.div
             className="h-full rounded-full bg-gold-500"
             animate={{ width: `${progress}%` }}
             transition={{ type: 'spring', stiffness: 200, damping: 30 }}
           />
         </div>
       </div>
       ```

    7. **Wrap question card with AnimatePresence + motion.div** — wrap ONLY the card (the `.card` div and its children), keeping top bar and progress OUTSIDE the animated wrapper:
       ```jsx
       <AnimatePresence mode="wait" custom={dirRef.current}>
         <motion.div
           key={currentIndex}
           custom={dirRef.current}
           variants={slideVariants}
           initial="enter"
           animate="center"
           exit="exit"
         >
           {/* The entire question card goes here */}
           <div className="rounded-2xl p-6 sm:p-7 border border-white/10" style={{ background: '#253d63' }}>
             {/* ALL existing card content unchanged — just replace className from "card p-6 sm:p-7" to above */}
           </div>
         </motion.div>
       </AnimatePresence>
       ```

    8. **Redesign — color changes inside the card** (palette: #1e3050 base, #253d63 cards, gold accents):
       - Question card wrapper: `className="card p-6 sm:p-7"` → use inline style card:
         `className="rounded-2xl p-6 sm:p-7 border border-white/10"` with `style={{ background: '#253d63' }}`
       - `<h2 className="text-2xl sm:text-[1.6rem] font-bold text-ink-900 ...">` → `text-white`
       - `<p className="text-ink-500 text-[15px] ...">` → `text-white/70`
       - Progress chapter label: `text-brand-600` → `text-gold-400`
       - Top bar "Voltar" button: `text-ink-400 hover:text-ink-700` → `text-white/60 hover:text-white`
       - "Capítulo X de Y" span: `text-ink-400` → `text-white/50`
       - "Pergunta X de Y" p: `text-ink-400` → `text-white/40`
       - Primary button: change `btn-primary` → `btn-gold`
       - Back button inside card: keep `btn-back` class
       - Footer text: `text-ink-400` → `text-white/35`
       - Single input wrapper: `bg-ink-50 rounded-lg p-5 mb-3 border border-ink-200 focus-within:border-brand-400 focus-within:bg-white` → `rounded-xl p-5 mb-3 border border-white/15 focus-within:border-gold-400 transition-all` with `style={{ background: 'rgba(255,255,255,0.06)' }}`
       - `<span className="text-2xl font-semibold text-ink-300 font-mono">R$</span>` → `text-white/40`
       - "Pode deixar em branco" text: `text-ink-400` → `text-white/40`
       - Example text `text-ink-400` → `text-white/35`
       - HelperTip button: `text-brand-600 hover:text-brand-700` → `text-gold-400 hover:text-gold-300`
       - HelperTip content box: `bg-brand-50 border-brand-100 text-ink-600` → `bg-gold-500/10 border-gold-500/20 text-white/70`
       - Choice options (isChoice): border-ink-200 → `border-white/15`; bg-white → inline `style={{ background: 'rgba(255,255,255,0.06)' }}`; hover:border-ink-300 → `hover:border-white/30`; selected money stays green; selected text money → keep; unselected label `text-ink-700` → `text-white/80`
       - ItemizedInput: item-row bg override → `style={{ background: 'rgba(255,255,255,0.06)', borderColor: 'rgba(255,255,255,0.12)' }}`; desc input text → `text-white/80`; value input → `text-white`
       - ItemizedInput total row: `border-ink-100` → `border-white/10`; `text-ink-400` → `text-white/40`; `text-ink-800` → `text-white`
       - `btn-add-item` class: keep as is

    9. **LiveDRE panel — #253d63 card on #1e3050 bg**:
       - Main card: `card p-5` → `rounded-2xl p-5 border border-white/10` with `style={{ background: '#253d63' }}`
       - `text-ink-400 uppercase` (DRE title) → `text-white/40`
       - `text-ink-800` (business name) → `text-white`
       - `border-ink-100` dividers → `border-white/10`
       - `text-ink-500` labels → `text-white/55`
       - `text-ink-700` values → `text-white/85`
       - `text-ink-800` "Lucro Líquido" → `text-white font-bold`
       - `text-ink-300` unfilled → `text-white/20`
       - `text-ink-400` margin % → `text-white/35`
       - `bg-navy-900` metric rows inside LiveDRE → inline `style={{ background: 'rgba(0,0,0,0.15)' }}`
       - Insight card: `card p-4` → `rounded-2xl p-4 border border-white/10` with `style={{ background: '#253d63' }}`
       - Insight title `text-ink-800` → `text-white`
       - Insight text `text-ink-500` → `text-white/55`
       - Live dot: `bg-money-500 animate-pulse` → `bg-gold-400 animate-pulse`

    10. **Outer wrapper** — change outer div className:
        ```jsx
        <div className="grid lg:grid-cols-[1.1fr_400px] gap-6 max-w-5xl w-full mx-auto">
        ```
        (remove `animate-slide-up` since AnimatePresence handles it now)
  </action>
  <verify>
    Manual: Navigate to questionnaire → advance a question → slide animates left. Go back → slide animates right.
    Progress bar spring-animates width on each step.
    Card is dark navy on each question.
  </verify>
  <acceptance_criteria>
    - `grep -n "AnimatePresence" frontend/src/components/Questionnaire.jsx` returns ≥1 line
    - `grep -n "slideVariants" frontend/src/components/Questionnaire.jsx` returns ≥1 line
    - `grep -n "motion.div" frontend/src/components/Questionnaire.jsx` returns ≥1 line
    - `grep -n "bg-navy-900" frontend/src/components/Questionnaire.jsx` returns ≥1 line
    - `grep -n "bg-gold-500" frontend/src/components/Questionnaire.jsx` returns ≥1 line (progress bar)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Questionnaire.jsx has slide animation + dark navy redesign. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.jsx wrapper background for questionnaire and loading steps</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx — read the wrapper div and STEPS const
  </read_first>
  <action>
    Update App.jsx wrapper div to use dark background when step is questionnaire or loading.

    Find this line (approximately line 220):
    ```jsx
    <div className={step === STEPS.LANDING ? '' : 'min-h-screen flex items-start sm:items-center justify-center p-4 py-8 bg-ink-50'}>
    ```

    Replace with:
    ```jsx
    <div
      className={
        step === STEPS.LANDING
          ? ''
          : 'min-h-screen flex items-start sm:items-center justify-center p-4 py-8'
      }
      style={
        step !== STEPS.LANDING && [STEPS.QUESTIONNAIRE, STEPS.LOADING].includes(step)
          ? { background: '#1e3050' }
          : step !== STEPS.LANDING
            ? {}
            : {}
      }
    >
    ```

    Wait — simpler approach, use a computed className:
    ```jsx
    <div className={
      step === STEPS.LANDING
        ? ''
        : [STEPS.QUESTIONNAIRE, STEPS.LOADING].includes(step)
          ? 'min-h-screen flex items-start sm:items-center justify-center p-4 py-8'
          : 'min-h-screen flex items-start sm:items-center justify-center p-4 py-8 bg-ink-50'
    }
    style={[STEPS.QUESTIONNAIRE, STEPS.LOADING].includes(step) ? { background: '#1e3050' } : {}}
    >
    ```

    This ensures:
    - LANDING: no wrapper (Landing handles its own bg)
    - QUESTIONNAIRE + LOADING: `#1e3050` blue (no flash between these two steps)
    - All other steps (auth, onboarding, diagnosis, etc.): existing `bg-ink-50` (unchanged)

    Do NOT change any other part of App.jsx.
  </action>
  <verify>
    <automated>cd "C:/Users/mateu/Desktop/mvp cont/frontend" && node -e "const fs=require('fs'); const c=fs.readFileSync('src/App.jsx','utf8'); if(!c.includes('bg-slate-950')) throw new Error('bg-slate-950 missing'); console.log('PASS: dark bg applied');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bg-slate-950" frontend/src/App.jsx` returns 1 line
    - `grep -n "STEPS.QUESTIONNAIRE, STEPS.LOADING" frontend/src/App.jsx` returns 1 line (or similar includes check)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>App.jsx wrapper uses bg-slate-950 for questionnaire and loading steps.</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/mateu/Desktop/mvp cont/frontend"
npm run build
```
Build must exit 0. No type errors. No missing imports.
</verification>

<success_criteria>
- AnimatePresence slide animation in Questionnaire.jsx (FL-01)
- Motion progress bar with spring in Questionnaire.jsx (FL-02)
- App.jsx wrapper uses bg-slate-950 for QUESTIONNAIRE + LOADING steps (FL-03 prep)
- Navy+gold visual for all questionnaire elements
- All existing logic (canProceed, handleNext, handleBack, itemized, choice) preserved
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-fluxo-questionario/03-01-SUMMARY.md` following the summary template.
</output>
