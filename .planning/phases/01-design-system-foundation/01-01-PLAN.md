---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.js
  - frontend/index.html
autonomous: true
requirements:
  - DS-01
  - DS-02

must_haves:
  truths:
    - "tailwind.config.js has a `gold` color scale with keys 50 through 900"
    - "gold-500 equals #F59E0B"
    - "a `shadow-gold` named shadow exists"
    - "Inter font is loading with weights 400 500 600 700 and display=swap"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "gold color tokens + shadow-gold"
      contains: "gold-500"
    - path: "frontend/index.html"
      provides: "Inter via Google Fonts"
      contains: "Inter"
  key_links:
    - from: "frontend/tailwind.config.js gold tokens"
      to: "frontend/src/index.css @apply classes"
      via: "Tailwind JIT compilation"
      pattern: "gold-\\d{2,3}"
---

<objective>
Add gold color scale (DS-01) and verify Inter font (DS-02).

Purpose: Downstream plans (Phase 2 landing page) need `gold-*` tokens to exist in Tailwind before they can be used in components and classes.
Output: tailwind.config.js with full gold scale + shadow-gold. index.html verified/updated with Inter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add gold color scale and shadow-gold to tailwind.config.js</name>
  <files>frontend/tailwind.config.js</files>

  <read_first>
    - frontend/tailwind.config.js — read fully before any edit. Understand existing structure (ink, money, brand, loss, forest, navy). Insertion point: after the `forest` block, before `navy`.
  </read_first>

  <action>
Open frontend/tailwind.config.js. In `theme.extend.colors`, add the `gold` block AFTER the `forest` block and BEFORE the `navy` block. Use exactly these values:

```js
// Dourado — identidade nova landing navy+gold
gold: {
  50:  '#FFFBEB',
  100: '#FEF3C7',
  200: '#FDE68A',
  300: '#FCD34D',
  400: '#FBBF24',
  500: '#F59E0B',
  600: '#D97706',
  700: '#B45309',
  800: '#92400E',
  900: '#78350F',
},
```

In `theme.extend.boxShadow`, add AFTER `money-lg` (the last existing shadow):

```js
'gold':    '0 4px 20px rgba(245,158,11,0.28)',
'gold-lg': '0 8px 32px rgba(245,158,11,0.36)',
```

Do NOT modify any existing keys. Do NOT change `navy`, `forest`, `ink`, `money`, `brand`, `loss`, fontFamily, letterSpacing, animation, or keyframes. Additions only (per D-02).
  </action>

  <verify>
    <automated>node -e "const c=require('./frontend/tailwind.config.js'); const g=c.default.theme.extend.colors.gold; console.assert(g['500']==='#F59E0B','gold-500 wrong'); console.assert(g['50']==='#FFFBEB','gold-50 wrong'); console.assert(g['900']==='#78350F','gold-900 wrong'); const s=c.default.theme.extend.boxShadow; console.assert(s['gold'],'shadow-gold missing'); console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - tailwind.config.js contains the string `gold:`
    - tailwind.config.js contains `'#F59E0B'` (gold-500 exact value)
    - tailwind.config.js contains `'#FFFBEB'` (gold-50)
    - tailwind.config.js contains `'#78350F'` (gold-900)
    - tailwind.config.js contains `'gold':` inside boxShadow (shadow-gold)
    - tailwind.config.js contains `rgba(245,158,11,0.28)` (shadow-gold value)
    - No existing key is removed or renamed (grep navy: still present, grep money: still present)
  </acceptance_criteria>

  <done>gold scale 50–900 present in tailwind.config.js with gold-500=#F59E0B; shadow-gold and shadow-gold-lg added; no existing tokens broken</done>
</task>

<task type="auto">
  <name>Task 2: Verify Inter font loading in index.html (DS-02)</name>
  <files>frontend/index.html</files>

  <read_first>
    - frontend/index.html — read fully before any edit. Check line ~28 for the Google Fonts link. Verify Inter weights 400, 500, 600, 700 are present and display=swap is set.
  </read_first>

  <action>
Read frontend/index.html. Locate the Google Fonts `<link>` tag (currently around line 28).

Check the href contains ALL of: `Inter:wght@400;500;600;700` and `display=swap`.

Current value is:
`family=Inter:wght@400;500;600;700;800`

This satisfies DS-02 — Inter is loaded with 400/500/600/700 (plus 800) and display=swap. No change is needed.

IF for any reason Inter is NOT in the link (e.g., file was modified), add it:
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,700;0,9..144,800;1,9..144,700&family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@500;600&display=swap" rel="stylesheet" />
```

Place inside `<head>`, before the closing `</head>` tag, after the favicon links.

Do NOT remove Fraunces or JetBrains Mono from the link.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('frontend/index.html','utf8'); console.assert(h.includes('Inter:wght'),'Inter missing'); console.assert(h.includes('display=swap'),'swap missing'); console.assert(h.includes('400'),'400 missing'); console.log('DS-02 OK');"</automated>
  </verify>

  <acceptance_criteria>
    - index.html contains `Inter:wght`
    - index.html contains `display=swap`
    - index.html contains `400` within the Google Fonts href
    - index.html contains `500` within the Google Fonts href
    - index.html contains `600` within the Google Fonts href
    - index.html contains `700` within the Google Fonts href
    - index.html contains `preconnect` to fonts.googleapis.com
  </acceptance_criteria>

  <done>Inter font verified loading with weights 400/500/600/700 and display=swap via Google Fonts preconnect pattern</done>
</task>

</tasks>

<verification>
After both tasks:
1. Run Tailwind build to confirm no config errors: `cd frontend && npx tailwindcss --input src/index.css --output /tmp/test-build.css --minify 2>&1 | head -5`
2. Confirm gold tokens compile: grep for `gold` classes in build output
3. Confirm Inter link intact in index.html
</verification>

<success_criteria>
- tailwind.config.js has `gold` with 10 keys (50–900), gold-500 = #F59E0B
- tailwind.config.js has `shadow-gold` = `0 4px 20px rgba(245,158,11,0.28)`
- index.html serves Inter 400/500/600/700 with display=swap
- No existing tokens, components, or styles modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md` with:
- What was added (gold scale values, shadow values)
- DS-01 and DS-02 status
- Any deviations from plan
</output>
