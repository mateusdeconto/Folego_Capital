---
phase: 04-tela-de-diagnostico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Diagnosis.jsx
autonomous: true
requirements: [DG-01, DG-02, DG-03]
must_haves:
  truths:
    - "Valores monetários animam de R$ 0 até o valor real ao montar Diagnosis (Lucro Líquido, Lucro Bruto, EBITDA, Ponto de Equilíbrio, Projeção 30 dias)"
    - "Seções do Diagnosis revelam progressivamente com stagger (header → health → alert → hero → markdown → benchmark → premium → break-even → projeção → export → ações)"
    - "Cards de alerta (HealthBadge warn/loss + AlertCard mistura) entram com shakeIn (eixo X spring), têm border-l-4 + shadow-md + dot animate-pulse"
    - "Bars do BenchmarkChart animam de width 0% até o valor alvo ao montar"
    - "Tipografia/spacing batem com UI-SPEC: businessName text-4xl, label 'Diagnóstico financeiro' text-sm uppercase tracking-wider, projection text-xl, HeroCard divider mt-4 pt-4"
    - "prefers-reduced-motion respeitado — sem stagger delays nem count-up animation"
  artifacts:
    - path: "frontend/src/components/Diagnosis.jsx"
      provides: "Diagnosis screen com Framer Motion stagger + useCountUp + alert differentiation"
      contains: "import { motion, useReducedMotion } from 'framer-motion'"
    - path: "frontend/src/components/Diagnosis.jsx"
      provides: "useCountUp hook module-scope"
      contains: "function useCountUp(target"
    - path: "frontend/src/components/Diagnosis.jsx"
      provides: "Variants module-scope: fadeUp, shakeIn, staggerContainer"
      contains: "const staggerContainer"
  key_links:
    - from: "wrapper root <motion.div>"
      to: "staggerContainer variants"
      via: "variants={staggerContainer} initial='hidden' animate='visible'"
      pattern: "variants=\\{staggerContainer\\}"
    - from: "useCountUp"
      to: "formatBRL output"
      via: "formatBRL(useCountUp(metrics.netProfit, 1200))"
      pattern: "formatBRL\\(.*useCountUp|useCountUp.*formatBRL"
    - from: "HealthBadge tone warn/loss"
      to: "border-l-4 + shadow-md + animate-pulse dot"
      via: "conditional className append baseado em healthStatus.tone"
      pattern: "border-l-4 border-l-(amber|loss)"
    - from: "BenchmarkChart bar"
      to: "motion.div animate width"
      via: "<motion.div initial={{ width: '0%' }} animate={{ width: ... }}"
      pattern: "initial=\\{\\{ width: '0%' \\}\\}"
---

<objective>
Redesign Diagnosis.jsx adding Framer Motion stagger reveal (DG-02), count-up animation for monetary values (DG-01), and differentiated alert card animations (DG-03). Includes typography/spacing fixes from UI-SPEC Implementation Notes. Mudanças cirúrgicas — preservar toda lógica existente (calcMetrics, formatBRL, TONE_CLASSES, export handlers, CorrectDataModal, BenchmarkPremium, etc.).

Purpose: Final phase do redesign visual v2.0 — completar tela de resultado com micro-interações que reforçam confiança e legibilidade dos números.
Output: frontend/src/components/Diagnosis.jsx atualizado, requirements DG-01, DG-02, DG-03 done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tela-de-diagnostico/04-CONTEXT.md
@.planning/phases/04-tela-de-diagnostico/04-UI-SPEC.md
@frontend/src/components/Diagnosis.jsx
@frontend/src/components/Loading.jsx
@frontend/src/components/Questionnaire.jsx
@frontend/tailwind.config.js
@frontend/src/index.css

<interfaces>
<!-- Funções/imports existentes em Diagnosis.jsx que executor reutiliza -->

From frontend/src/lib/metrics.js:
```js
export function calcMetrics(financialData): { netProfit, netMargin, grossProfit, grossMargin, ebitda, breakEven, revenue, cogs };
export function formatBRL(value: number): string;  // "R$ 1.234,56" — handles negative
```

From frontend/src/components/Diagnosis.jsx (existing, preserve):
```js
// linha 134
function BenchmarkChart({ metrics, segment }) { ... }
// linha 190
const TONE_CLASSES = { money, brand, warn, loss };  // each { bg, border, text, dot }
// linha 495
export default function Diagnosis({ businessData, financialData, diagnosis, ... }) { ... }
```

From framer-motion (já em package.json):
```js
import { motion, useReducedMotion } from 'framer-motion';
// motion.div: aceita variants, initial, animate, transition props
// useReducedMotion(): boolean — true se OS prefer-reduced-motion ativo
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Framer Motion imports, module-scope variants, and useCountUp hook to Diagnosis.jsx</name>
  <files>frontend/src/components/Diagnosis.jsx</files>
  <read_first>
    - frontend/src/components/Diagnosis.jsx (entire file — 887 lines, must preserve all existing logic)
    - frontend/src/components/Questionnaire.jsx (linha 1-30: padrão de imports framer-motion + variants module-scope)
    - frontend/src/components/Loading.jsx (padrão Framer Motion estabelecido na Fase 3)
    - .planning/phases/04-tela-de-diagnostico/04-CONTEXT.md (decisões D-04 a D-13)
    - .planning/phases/04-tela-de-diagnostico/04-UI-SPEC.md (seções "Animation Contract" e "Implementation Notes")
  </read_first>
  <action>
    Modificar Diagnosis.jsx adicionando 3 blocos NO TOPO do arquivo, sem tocar em nada abaixo da linha 134 ainda.

    **A. Atualizar import line 1.** Linha atual:
    ```js
    import { useEffect, useMemo, useState } from 'react';
    ```
    Adicionar logo abaixo:
    ```js
    import { motion, useReducedMotion } from 'framer-motion';
    ```

    **B. Adicionar variants module-scope.** Inserir DEPOIS dos imports e ANTES de `function renderMarkdown` (linha ~7). Bloco exato:
    ```js
    // Framer Motion variants — module scope, reuse across all sections
    const fadeUp = {
      hidden:  { opacity: 0, y: 16 },
      visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: [0.16, 1, 0.3, 1] } },
    };

    const shakeIn = {
      hidden:  { opacity: 0, x: -8 },
      visible: { opacity: 1, x: 0, transition: { type: 'spring', stiffness: 300, damping: 20 } },
    };

    const staggerContainer = {
      hidden:  {},
      visible: { transition: { staggerChildren: 0.1, delayChildren: 0 } },
    };

    const staggerContainerReduced = {
      hidden:  {},
      visible: { transition: { staggerChildren: 0, delayChildren: 0 } },
    };
    ```

    **C. Adicionar useCountUp hook.** Inserir IMEDIATAMENTE ANTES de `function BenchmarkChart` (linha ~134). Bloco exato:
    ```js
    // Count-up hook — RAF + easeOutCubic. Handles negative targets via abs+sign.
    function useCountUp(target, duration = 1200, disabled = false) {
      const [value, setValue] = useState(disabled ? target : 0);
      useEffect(() => {
        if (disabled) { setValue(target); return; }
        const safeTarget = Number.isFinite(target) ? target : 0;
        if (safeTarget === 0) { setValue(0); return; }
        const sign = Math.sign(safeTarget);
        const abs = Math.abs(safeTarget);
        let start = null;
        let rafId;
        const step = (ts) => {
          if (!start) start = ts;
          const p = Math.min((ts - start) / duration, 1);
          const eased = 1 - Math.pow(1 - p, 3); // easeOutCubic
          setValue(abs * eased * sign);
          if (p < 1) rafId = requestAnimationFrame(step);
        };
        rafId = requestAnimationFrame(step);
        return () => { if (rafId) cancelAnimationFrame(rafId); };
      }, [target, duration, disabled]);
      return value;
    }
    ```

    Por que `disabled` flag: passa o resultado de `useReducedMotion()` no componente principal — quando true, retorna target direto sem RAF (D-13).

    NÃO modificar nada mais ainda. NÃO tocar em BenchmarkChart, TONE_CLASSES, Diagnosis default export, ou qualquer markup nesta task.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/components/Diagnosis.jsx','utf8'); const checks=[/import \{ motion, useReducedMotion \} from 'framer-motion'/,/const fadeUp = \{/,/const shakeIn = \{/,/const staggerContainer = \{/,/function useCountUp\(target/,/easeOutCubic/,/Math\.sign\(safeTarget\)/]; const miss=checks.filter(r=>!r.test(s)); if(miss.length){console.error('MISSING:',miss.map(r=>r.source));process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { motion, useReducedMotion } from 'framer-motion'" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep -n "const fadeUp = {" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep -n "const shakeIn = {" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep -n "const staggerContainer = {" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep -n "function useCountUp(target" frontend/src/components/Diagnosis.jsx` returns 1 match positioned BEFORE `function BenchmarkChart`
    - `grep -c "easeOutCubic" frontend/src/components/Diagnosis.jsx` >= 1
    - File line count increased by ~30-40 lines (no existing content deleted)
    - `cd frontend && npm run build` exits 0 (no syntax errors)
  </acceptance_criteria>
  <done>
    Imports, variants, e useCountUp hook adicionados no module scope. Build passa. Nenhuma lógica existente foi removida.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Animate BenchmarkChart bars + typography/spacing fixes in Diagnosis component</name>
  <files>frontend/src/components/Diagnosis.jsx</files>
  <read_first>
    - frontend/src/components/Diagnosis.jsx (releitura — Task 1 já modificou topo)
    - .planning/phases/04-tela-de-diagnostico/04-CONTEXT.md (decisões D-14 a D-19)
    - .planning/phases/04-tela-de-diagnostico/04-UI-SPEC.md (seções "Typography", "Spacing Scale", "Component Inventory > BenchmarkChart")
  </read_first>
  <action>
    Mudanças cirúrgicas em 5 pontos exatos. NÃO mexer em nada mais.

    **A. BenchmarkChart bars (linhas 170 e 176) — D-14.**

    Linha 170, ATUAL:
    ```jsx
    <div className={`h-full rounded-full ${userColor}`} style={{ width: `${userW}%` }} />
    ```
    SUBSTITUIR por:
    ```jsx
    <motion.div
      className={`h-full rounded-full ${userColor}`}
      initial={{ width: '0%' }}
      animate={{ width: `${userW}%` }}
      transition={{ duration: 0.6, ease: 'easeOut', delay: 0.4 }}
    />
    ```

    Linha 176, ATUAL:
    ```jsx
    <div className="h-full rounded-full bg-brand-300" style={{ width: `${sectW}%` }} />
    ```
    SUBSTITUIR por:
    ```jsx
    <motion.div
      className="h-full rounded-full bg-brand-300"
      initial={{ width: '0%' }}
      animate={{ width: `${sectW}%` }}
      transition={{ duration: 0.6, ease: 'easeOut', delay: 0.5 }}
    />
    ```

    **B. Label "Você vs. média do setor" (linha 146) — D-19.**
    ATUAL: `<p className="text-xs font-semibold text-ink-400 uppercase tracking-wider mb-4">`
    NOVO: `<p className="text-sm font-bold text-ink-400 uppercase tracking-wider mb-4">`
    (text-xs → text-sm, font-semibold → font-bold)

    **C. Header "Diagnóstico financeiro" label (linha 594) — D-16.**
    ATUAL:
    ```jsx
    <p className="text-xs font-medium text-ink-400 uppercase tracking-wider mb-2">
      Diagnóstico financeiro
    </p>
    ```
    NOVO:
    ```jsx
    <p className="text-sm font-bold text-ink-400 uppercase tracking-wider mb-2">
      Diagnóstico financeiro
    </p>
    ```

    **D. Header businessName H1 (linha 597) — D-15.**
    ATUAL: `<h1 className="text-3xl font-bold text-ink-900 tracking-tighter">`
    NOVO: `<h1 className="text-4xl font-bold text-ink-900 tracking-tighter">`

    **E. HeroCard divider spacing (linha 658) — D-18.**
    ATUAL: `<div className="grid grid-cols-2 gap-3 mt-5 pt-5 border-t border-ink-800">`
    NOVO: `<div className="grid grid-cols-2 gap-3 mt-4 pt-4 border-t border-ink-800">`

    **F. ProjectionCard value size (linha 724) — D-17.**
    ATUAL: `<p className={`text-2xl font-bold tracking-tighter font-mono mb-1 ${projTone.text}`}>`
    NOVO: `<p className={`text-xl font-bold tracking-tighter font-mono mb-1 ${projTone.text}`}>`

    NÃO adicionar motion wrappers em cards ainda — isso é Task 3.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/components/Diagnosis.jsx','utf8'); const checks=[[/initial=\{\{ width: '0%' \}\}/g,2,'bar motion.div initial'],[/animate=\{\{ width: `\$\{userW\}%` \}\}/,1,'userW animate'],[/animate=\{\{ width: `\$\{sectW\}%` \}\}/,1,'sectW animate'],[/Diagnóstico financeiro/,1,'label text'],[/text-4xl font-bold text-ink-900 tracking-tighter/,1,'h1 text-4xl'],[/mt-4 pt-4 border-t border-ink-800/,1,'hero divider'],[/text-xl font-bold tracking-tighter font-mono mb-1 \$\{projTone\.text\}/,1,'projection text-xl']]; let fail=0; for(const [re,n,name] of checks){const m=s.match(new RegExp(re,'g'))||[]; if(m.length<n){console.error('FAIL',name,'expected>=',n,'got',m.length);fail++}} const bad=[/<h1 className=\"text-3xl/,/text-xs font-medium text-ink-400 uppercase tracking-wider mb-2\">[\\s]*Diagnóstico/,/mt-5 pt-5 border-t border-ink-800/,/text-2xl font-bold tracking-tighter font-mono mb-1/]; for(const re of bad){if(re.test(s)){console.error('STILL PRESENT',re.source);fail++}} if(fail)process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initial={{ width: '0%' }}" frontend/src/components/Diagnosis.jsx` returns 2 (userBar + sectorBar)
    - `grep -c "transition={{ duration: 0.6, ease: 'easeOut'" frontend/src/components/Diagnosis.jsx` >= 2
    - `grep "text-4xl font-bold text-ink-900 tracking-tighter" frontend/src/components/Diagnosis.jsx` finds the businessName H1
    - `grep "text-3xl font-bold text-ink-900 tracking-tighter" frontend/src/components/Diagnosis.jsx` returns NO match (old size removed)
    - `grep "text-sm font-bold text-ink-400 uppercase tracking-wider mb-2" frontend/src/components/Diagnosis.jsx` finds "Diagnóstico financeiro" label
    - `grep "mt-5 pt-5 border-t border-ink-800" frontend/src/components/Diagnosis.jsx` returns NO match
    - `grep "mt-4 pt-4 border-t border-ink-800" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "text-xl font-bold tracking-tighter font-mono mb-1" frontend/src/components/Diagnosis.jsx` finds ProjectionCard value
    - `grep "text-2xl font-bold tracking-tighter font-mono mb-1" frontend/src/components/Diagnosis.jsx` returns NO match
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    BenchmarkChart bars animam de 0% à largura alvo. Tipografia e spacing batem com UI-SPEC: businessName 4xl, label "Diagnóstico financeiro" sm bold uppercase, ProjectionCard xl, HeroCard divider mt-4 pt-4. Build passa.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wrap Diagnosis sections with motion stagger + count-up monetary values + alert card differentiation</name>
  <files>frontend/src/components/Diagnosis.jsx</files>
  <read_first>
    - frontend/src/components/Diagnosis.jsx (releitura completa — Tasks 1 e 2 modificaram topo + BenchmarkChart + tipografia)
    - .planning/phases/04-tela-de-diagnostico/04-CONTEXT.md (decisões D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/04-tela-de-diagnostico/04-UI-SPEC.md (seção "Animation Contract" + tabela stagger delays)
  </read_first>
  <action>
    Modificar APENAS o JSX retornado pelo `export default function Diagnosis` (linhas 590+). NÃO mexer em BenchmarkChart, TONE_CLASSES, ou funções utilitárias.

    **A. Inserir hook reducedMotion no topo do componente Diagnosis (logo após `const isPaid = plan === 'paid';` linha 507).**
    ```js
    const shouldReduceMotion = useReducedMotion();
    ```

    **B. Calcular count-up values logo APÓS as constantes `netProfitPositive`, `healthTone`, `projTone` (linhas 522-524). Adicionar bloco:**
    ```js
    // Count-up animated values (DG-01) — disabled if user prefers reduced motion
    const animatedNetProfit  = useCountUp(metrics.netProfit, 1200, shouldReduceMotion);
    const animatedGrossProfit = useCountUp(metrics.grossProfit, 1000, shouldReduceMotion);
    const animatedEbitda      = useCountUp(metrics.ebitda, 1000, shouldReduceMotion);
    const animatedBreakEven   = useCountUp(metrics.breakEven, 1000, shouldReduceMotion);
    const animatedProjection  = useCountUp(projection.projected, 900, shouldReduceMotion);

    const isAlertHealth = healthStatus && (healthStatus.tone === 'warn' || healthStatus.tone === 'loss');
    const healthBorderLeftClass = isAlertHealth
      ? (healthStatus.tone === 'warn' ? 'border-l-4 border-l-amber-400' : 'border-l-4 border-l-loss-500')
      : '';
    const healthShadowClass = isAlertHealth ? 'shadow-md' : '';
    const healthDotPulse    = isAlertHealth ? 'animate-pulse' : '';
    ```

    **C. Substituir wrapper root (linha 591).**
    ATUAL: `<div className="animate-slide-up space-y-4">`
    NOVO:
    ```jsx
    <motion.div
      className="space-y-4"
      variants={shouldReduceMotion ? staggerContainerReduced : staggerContainer}
      initial="hidden"
      animate="visible"
    >
    ```
    E a tag `</div>` correspondente no fim do return → `</motion.div>`.

    **D. Header (linhas 593-606).** Trocar `<div className="mb-1">` por `<motion.div className="mb-1" variants={fadeUp}>` e `</div>` correspondente por `</motion.div>`.

    **E. Health badge (linhas 609-617).** Atual:
    ```jsx
    {healthStatus && (
      <div className={`flex items-center gap-3 p-4 rounded-lg border ${healthTone.bg} ${healthTone.border}`}>
        <span className={`w-2.5 h-2.5 rounded-full ${healthTone.dot}`} />
        ...
      </div>
    )}
    ```
    NOVO:
    ```jsx
    {healthStatus && (
      <motion.div
        className={`flex items-center gap-3 p-4 rounded-lg border ${healthTone.bg} ${healthTone.border} ${healthBorderLeftClass} ${healthShadowClass}`}
        variants={isAlertHealth ? shakeIn : fadeUp}
      >
        <span className={`w-2.5 h-2.5 rounded-full ${healthTone.dot} ${healthDotPulse}`} />
        ...
      </motion.div>
    )}
    ```
    (preservar conteúdo interno do `<div className="flex-1">...`)

    **F. Mistura financeira AlertCard (linhas 620-636).** Atual:
    ```jsx
    {financialData.mixedAccounts && (
      <div className="rounded-lg p-4 border border-loss-200 bg-loss-50">
        ...
      </div>
    )}
    ```
    NOVO:
    ```jsx
    {financialData.mixedAccounts && (
      <motion.div
        className="rounded-lg p-4 border border-loss-200 bg-loss-50 border-l-4 border-l-loss-500 shadow-md"
        variants={shakeIn}
      >
        ...
      </motion.div>
    )}
    ```

    **G. HeroCard Lucro Líquido (linhas 638-672).**
    - Trocar `<div className="card-dark p-6">` → `<motion.div className="card-dark p-6" variants={fadeUp}>` e `</div>` correspondente para `</motion.div>`.
    - Linha 651 (valor netProfit): trocar `{formatBRL(metrics.netProfit)}` por `{formatBRL(animatedNetProfit)}`.
    - Linha 661 (grossProfit): trocar `{formatBRL(metrics.grossProfit)}` por `{formatBRL(animatedGrossProfit)}`.
    - Linha 666 (ebitda): trocar `{formatBRL(metrics.ebitda)}` por `{formatBRL(animatedEbitda)}`.
    - NÃO mexer em margens percentuais (netMargin, grossMargin) — permanecem estáticas (D-11).

    **H. Diagnóstico Markdown card (linhas 674-677).**
    ATUAL: `<div className="card p-6">`
    NOVO: `<motion.div className="card p-6" variants={fadeUp}>` (e `</div>` correspondente → `</motion.div>`)

    **I. BenchmarkChart wrapper (linha 680).**
    Wrap a chamada `<BenchmarkChart ... />` num motion.div:
    ```jsx
    <motion.div variants={fadeUp}>
      <BenchmarkChart metrics={metrics} segment={businessData.segment} />
    </motion.div>
    ```

    **J. BenchmarkPremium wrapper (linhas 683-689).** Mesmo padrão:
    ```jsx
    <motion.div variants={fadeUp}>
      <BenchmarkPremium ... />
    </motion.div>
    ```

    **K. BreakEven card (linhas 692-712).**
    ATUAL: `<div className="card p-5 border-2 border-brand-200 bg-gradient-to-br from-brand-50 to-white">`
    NOVO: `<motion.div className="..." variants={fadeUp}>` (mesma classe, fechar com `</motion.div>`)
    - Linha 696 trocar `{formatBRL(metrics.breakEven)}` por `{formatBRL(animatedBreakEven)}` (apenas a primeira ocorrência do valor display grande; as referências em strings de prosa nas linhas 698, 706, 707 permanecem estáticas).

    **L. ProjectionCard (linhas 714-...).**
    ATUAL: `<div className="card p-5">`
    NOVO: `<motion.div className="card p-5" variants={fadeUp}>` (fechar com `</motion.div>`)
    - Linha 725 trocar `{formatBRL(projection.projected)}` por `{formatBRL(animatedProjection)}`.
    - Demais valores no breakdown "Como calculamos" permanecem estáticos.

    **M. Restante dos cards (ExportCard / WhatsApp share / ActionButtons).** Para cada `<div className="card ...">` ou container de botões que aparece como filho direto do wrapper raiz `<motion.div className="space-y-4">`, trocar para `<motion.div className="..." variants={fadeUp}>` e fechar com `</motion.div>`. Aplicar UM motion.div por seção filha direta — NÃO aninhar variants em filhos internos.

    **N. CorrectDataModal e UpgradeModal renderizados como portals/conditionals no final do return — NÃO envolver em motion.div** (modals têm próprio animate-slide-up CSS, fora do stagger).

    **Edge cases.**
    - useCountUp recebe `Number.isFinite` guard — para metrics undefined no primeiro render, retornará 0. Como `useMemo` resolve métricas sincronamente no mount, isso não deve disparar.
    - `shouldReduceMotion`: passa true ao useCountUp via 3o arg → retorna target direto.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/components/Diagnosis.jsx','utf8'); const must=[/const shouldReduceMotion = useReducedMotion\(\)/,/const animatedNetProfit\s*= useCountUp\(metrics\.netProfit, 1200, shouldReduceMotion\)/,/const animatedGrossProfit\s*= useCountUp\(metrics\.grossProfit, 1000, shouldReduceMotion\)/,/const animatedEbitda\s*= useCountUp\(metrics\.ebitda, 1000, shouldReduceMotion\)/,/const animatedBreakEven\s*= useCountUp\(metrics\.breakEven, 1000, shouldReduceMotion\)/,/const animatedProjection\s*= useCountUp\(projection\.projected, 900, shouldReduceMotion\)/,/variants=\{shouldReduceMotion \? staggerContainerReduced : staggerContainer\}/,/formatBRL\(animatedNetProfit\)/,/formatBRL\(animatedGrossProfit\)/,/formatBRL\(animatedEbitda\)/,/formatBRL\(animatedBreakEven\)/,/formatBRL\(animatedProjection\)/,/variants=\{isAlertHealth \? shakeIn : fadeUp\}/,/border-l-4 border-l-loss-500 shadow-md/,/healthDotPulse/]; const banned=[/className=\"animate-slide-up space-y-4\"/]; let fail=0; for(const re of must){if(!re.test(s)){console.error('MISSING',re.source);fail++}} for(const re of banned){if(re.test(s)){console.error('STILL PRESENT (should be removed)',re.source);fail++}} const motionCount=(s.match(/<motion\.div/g)||[]).length; if(motionCount<8){console.error('motion.div count too low:',motionCount,'expected>=8');fail++} if(fail)process.exit(1); console.log('OK motion.div count:',motionCount)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "const shouldReduceMotion = useReducedMotion()" frontend/src/components/Diagnosis.jsx` returns 1 match inside the default export function (after `const isPaid`)
    - `grep "useCountUp(metrics.netProfit, 1200, shouldReduceMotion)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "useCountUp(metrics.grossProfit, 1000, shouldReduceMotion)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "useCountUp(metrics.ebitda, 1000, shouldReduceMotion)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "useCountUp(metrics.breakEven, 1000, shouldReduceMotion)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "useCountUp(projection.projected, 900, shouldReduceMotion)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "formatBRL(animatedNetProfit)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "formatBRL(animatedGrossProfit)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "formatBRL(animatedEbitda)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "formatBRL(animatedBreakEven)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "formatBRL(animatedProjection)" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "animate-slide-up space-y-4" frontend/src/components/Diagnosis.jsx` returns NO match (root class replaced)
    - `grep -c "<motion.div" frontend/src/components/Diagnosis.jsx` >= 8 (root + header + healthBadge + alert + heroCard + markdown + benchmarkWrap + benchmarkPremiumWrap + breakEven + projection + actions ≈ 10-11)
    - `grep "variants={isAlertHealth ? shakeIn : fadeUp}" frontend/src/components/Diagnosis.jsx` returns 1 match
    - `grep "border-l-4 border-l-loss-500 shadow-md" frontend/src/components/Diagnosis.jsx` returns >= 1 match (AlertCard mixedAccounts)
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm run dev` boots without runtime errors (manual sanity if available)
  </acceptance_criteria>
  <done>
    Diagnosis screen reveals com stagger ao montar (DG-02). Valores Lucro Líquido, Lucro Bruto, EBITDA, Ponto de Equilíbrio, Projeção 30d animam de 0 ao valor real (DG-01). HealthBadge warn/loss + AlertCard mixed accounts têm shakeIn entrance + border-l-4 + shadow-md + dot pulse (DG-03). prefers-reduced-motion respeitado. Build limpo.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npm run dev` carrega Diagnosis sem console errors
- Manual smoke: completar questionário → ver Diagnosis → confirmar (a) cards aparecem sequencialmente em stagger, (b) números monetários contam de R$ 0 ao valor, (c) HealthBadge "Atenção" ou "Crítica" tem borda esquerda colorida e dot pulsando, (d) bars de benchmark crescem de 0 ao valor
- Test reduced motion: DevTools → Rendering → Emulate CSS prefers-reduced-motion: reduce → recarregar Diagnosis → valores aparecem instantaneamente sem contagem, sem stagger
</verification>

<success_criteria>
- DG-01: 5 valores monetários (netProfit, grossProfit, ebitda, breakEven, projection.projected) animam de 0 ao target em mount
- DG-02: Wrapper raiz é motion.div com staggerContainer; cards filhos têm variants={fadeUp} ou {shakeIn}; sequência visível em DevTools Animations panel
- DG-03: HealthBadge warn/loss e AlertCard mixedAccounts usam shakeIn + border-l-4 + shadow-md + dot animate-pulse
- Tipografia: businessName text-4xl, label "Diagnóstico financeiro" text-sm font-bold uppercase, ProjectionCard text-xl, HeroCard divider mt-4 pt-4
- BenchmarkChart bars animate width 0% → target ao montar
- prefers-reduced-motion: stagger e count-up desabilitados
- Zero regressões: export PDF/Excel, modal CorrectData, UpgradeModal, BenchmarkPremium tudo funcionando
</success_criteria>

<output>
After completion, create `.planning/phases/04-tela-de-diagnostico/04-01-SUMMARY.md` documenting:
- Patches applied (imports, variants, hook, JSX wrappers, typography fixes)
- Line count delta
- Animations verified manually
- Any deviations from plan (with reason)
</output>
