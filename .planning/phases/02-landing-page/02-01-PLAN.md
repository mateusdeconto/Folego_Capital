---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.js
autonomous: true
requirements:
  - LP-01
must_haves:
  truths:
    - "CSS class `animate-[glow-pulse_10s_ease-in-out_infinite]` resolves without Tailwind warning"
    - "Two glow orbs render with opacity oscillating between 0.10 and 0.15 in browser"
  artifacts:
    - path: frontend/tailwind.config.js
      provides: "glow-pulse keyframe + animation token"
      contains: "glow-pulse"
  key_links:
    - from: frontend/tailwind.config.js
      to: frontend/src/components/Landing.jsx
      via: "Tailwind JIT generates animate-[glow-pulse_*] utility class"
      pattern: "glow-pulse"
---

<objective>
Add `glow-pulse` CSS keyframe and animation token to tailwind.config.js so the hero background "luz cortando névoa" animation can be consumed by class utilities in Landing.jsx.

Purpose: Enables LP-01 (hero animated background). Without this keyframe, `animate-[glow-pulse_10s_ease-in-out_infinite]` produces no animation.
Output: `frontend/tailwind.config.js` updated with `glow-pulse` keyframe and animation entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current tailwind.config.js keyframes/animation blocks (Phase 1 output) -->
<!-- From frontend/tailwind.config.js lines 104–116 -->

animation: {
  'fade-in':    'fadeIn 0.25s ease-out',
  'slide-up':   'slideUp 0.35s cubic-bezier(0.16, 1, 0.3, 1)',
  'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
  'spin-slow':  'spin 2s linear infinite',
  'float':      'float 6s ease-in-out infinite',
},

keyframes: {
  fadeIn:  { '0%': { opacity: '0' }, '100%': { opacity: '1' } },
  slideUp: { '0%': { opacity: '0', transform: 'translateY(10px)' }, '100%': { opacity: '1', transform: 'translateY(0)' } },
  float:   { '0%,100%': { transform: 'translateY(0)' }, '50%': { transform: 'translateY(-8px)' } },
},
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add glow-pulse keyframe and animation token to tailwind.config.js</name>
  <files>frontend/tailwind.config.js</files>
  <read_first>
    - frontend/tailwind.config.js — read current file before editing; preserve ALL existing keyframes and animations
  </read_first>
  <action>
    Add two entries to the existing `tailwind.config.js`:

    1. In the `keyframes` object, add after the existing `float` entry:
    ```js
    'glow-pulse': {
      '0%, 100%': { opacity: '0.10', transform: 'scale(1)' },
      '50%':      { opacity: '0.15', transform: 'scale(1.08)' },
    },
    ```

    2. In the `animation` object, add after the existing `float` entry:
    ```js
    'glow-pulse': 'glow-pulse 10s ease-in-out infinite',
    ```

    Per D-04 (locked): keyframes go in `tailwind.config.js > keyframes`, never as inline CSS.
    Per D-01/D-03 (locked): scale(1→1.08) + opacity(0.10→0.15), 10s cycle.

    Do NOT change any other part of the file.
  </action>
  <verify>
    <automated>cd "C:/Users/mateu/Desktop/mvp cont/frontend" && node -e "const c=require('./tailwind.config.js'); const kf=c.default.theme.extend.keyframes; const an=c.default.theme.extend.animation; if(!kf['glow-pulse']) throw new Error('keyframe missing'); if(!an['glow-pulse']) throw new Error('animation missing'); console.log('PASS: glow-pulse keyframe and animation present');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "glow-pulse" frontend/tailwind.config.js` returns exactly 2 lines (one in keyframes, one in animation)
    - Keyframe entry contains `opacity: '0.10'` at 0%/100% and `opacity: '0.15'` at 50%
    - Keyframe entry contains `transform: 'scale(1)'` at 0%/100% and `transform: 'scale(1.08)'` at 50%
    - Animation entry value is `'glow-pulse 10s ease-in-out infinite'`
    - `npm run build` exits 0 with no errors after the change
  </acceptance_criteria>
  <done>tailwind.config.js exports a `glow-pulse` keyframe and animation; `npm run build` succeeds.</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/mateu/Desktop/mvp cont/frontend"
npm run build
```
Build must exit 0. No warnings about unknown animation names.
</verification>

<success_criteria>
- `frontend/tailwind.config.js` contains `glow-pulse` in both `keyframes` and `animation` blocks
- All existing keyframes/animations preserved (fadeIn, slideUp, float, fade-in, slide-up, float, pulse-slow, spin-slow)
- `npm run build` passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md` following the summary template.
</output>
