---
phase: 02-landing-page
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/src/components/Landing.jsx
autonomous: false
requirements:
  - LP-05
  - LP-06
  - LP-07
  - LP-08
  - LP-09
must_haves:
  truths:
    - "Pain points section renders 6 glass-card items with scroll-triggered stagger entry"
    - "Como Funciona renders 3 numbered steps with scroll-triggered stagger and connector line on desktop"
    - "Features renders 6 card-navy cards in a 3-column grid with scroll-triggered stagger"
    - "FAQ renders 5 items with animated accordion open/close (AnimatePresence height animation)"
    - "Footer renders with navy-950 background, Logo, tagline, and email link"
    - "Full page build passes with 0 errors"
  artifacts:
    - path: frontend/src/components/Landing.jsx
      provides: "Complete landing page — all 9 LP sections present"
      contains: "FAQS.map"
    - path: frontend/src/components/Landing.jsx
      provides: "FAQ accordion with AnimatePresence"
      contains: "AnimatePresence"
  key_links:
    - from: "Pain section motion.div"
      to: "PAIN_POINTS array"
      via: "PAIN_POINTS.map(p => motion.div variants={fadeUp})"
      pattern: "PAIN_POINTS.map"
    - from: "FAQ FaqItem"
      to: "AnimatePresence"
      via: "AnimatePresence initial={false} wrapping motion.div height animation"
      pattern: "AnimatePresence"
---

<objective>
Complete `frontend/src/components/Landing.jsx` by replacing the `{/* TODO Plan 03 */}` stub with all remaining sections: Pain Points, Como Funciona, Features, CTA Final, FAQ, and Footer. After this plan, the full landing page is complete and all LP-01 through LP-09 requirements are met.

Purpose: Delivers LP-05 through LP-09 (pain section, steps, features, FAQ, footer) and produces the final complete Landing.jsx.
Output: Complete `Landing.jsx` with all sections, all scroll animations, full navy/gold palette, zero money-* classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md
@.planning/phases/02-landing-page/02-UI-SPEC.md
@.planning/phases/02-landing-page/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Patterns to reuse in this plan -->

<!-- InView component (already in file from Plan 02) -->
function InView({ children, delay = 0, className = '', once = true })
// uses useInView(ref, { once, margin: '-60px' }) + motion.div fadeUp

<!-- staggerContainer usage pattern -->
variants={staggerContainer(0.07)} // pain, features
variants={staggerContainer(0.12)} // steps

<!-- CSS classes from index.css -->
.glass-card    // pain items: rgba(255,255,255,0.03) bg + hover lifts to 0.06
.card-navy     // features cards: navy-800 bg, white/10 border, hover gold-500/30
.landing-container        // max-w-6xl mx-auto px-6
.landing-container-narrow // max-w-2xl mx-auto px-6
.btn-gold                 // primary CTA

<!-- Color tokens for dark sections -->
text-gold-400     // section labels, step numbers, icon accents
text-white        // primary text
text-white/65     // secondary text
text-white/50     // muted text
border-white/10   // dividers
bg-navy-900       // section backgrounds (darker)
bg-navy-800       // card backgrounds
bg-navy-950       // footer background (#030810)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Pain, Como Funciona, Features sections into Landing.jsx</name>
  <files>frontend/src/components/Landing.jsx</files>
  <read_first>
    - frontend/src/components/Landing.jsx — read the current file to find the `{/* TODO Plan 03 */}` comment and understand exact insertion point; confirm PAIN_POINTS, STEPS_FLOW, FEATURES arrays are present
    - .planning/phases/02-landing-page/02-UI-SPEC.md — Component Inventory: Pain Points, Como Funciona, Features specs
    - .planning/phases/02-landing-page/02-RESEARCH.md — Pattern 5 (scroll-triggered sections), Anti-Patterns section
  </read_first>
  <action>
    Replace the `{/* TODO Plan 03: Pain, Como Funciona, Features, FAQ, CTA Final, Footer */}` comment with the following sections. Keep remaining `{/* TODO */}` stubs for FAQ, CTA Final, Footer — Task 2 handles those.

    **Pain Points section (LP-05):**
    ```jsx
    <section className="landing-container py-24 sm:py-32">
      <div className="max-w-3xl mx-auto">
        <InView className="mb-12">
          <div className="flex items-center gap-3 mb-4">
            <span className="w-8 h-px bg-gold-500/60" />
            <span className="text-xs font-bold text-gold-400 uppercase tracking-widest">
              POR QUE DONO DE NEGÓCIO NÃO DORME
            </span>
          </div>
          <h2 className="text-3xl sm:text-[2.6rem] font-bold text-white tracking-tighter leading-tight">
            Tocar um negócio sem ler<br className="hidden sm:block" /> os números é dirigir de olhos fechados.
          </h2>
        </InView>

        <motion.div
          className="space-y-2"
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, margin: '-60px' }}
          variants={staggerContainer(0.055)}
        >
          {PAIN_POINTS.map(p => (
            <motion.div
              key={p}
              variants={fadeUp}
              className="group flex items-start gap-4 p-4 glass-card rounded-2xl transition-colors duration-200"
              whileHover={{ borderColor: 'rgba(251,191,36,0.20)' }}
            >
              <div className="w-6 h-6 rounded-full bg-gold-500/10 border border-gold-500/20
                              flex items-center justify-center flex-shrink-0 mt-0.5">
                <span className="w-2 h-2 rounded-full bg-gold-400" />
              </div>
              <p className="text-[15px] text-white/75 leading-relaxed">{p}</p>
            </motion.div>
          ))}
        </motion.div>

        <InView delay={100}>
          <div className="mt-10 p-5 bg-gold-500/10 border border-gold-500/20 rounded-2xl flex items-center gap-4">
            <div className="w-10 h-10 rounded-xl bg-gold-500 flex items-center justify-center flex-shrink-0">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                <path d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                  stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
            <p className="text-sm font-semibold text-white/80">
              Se marcou pelo menos um, é exatamente pra isso que o FinCheck existe.
            </p>
          </div>
        </InView>
      </div>
    </section>
    ```

    **Como Funciona section (LP-06):**
    ```jsx
    <section id="como-funciona" className="bg-navy-900 py-24 sm:py-32">
      <div className="landing-container">
        <InView className="mb-14">
          <div className="flex items-center gap-3 mb-4">
            <span className="w-8 h-px bg-gold-500/60" />
            <span className="text-xs font-bold text-gold-400 uppercase tracking-widest">
              COMO FUNCIONA
            </span>
          </div>
          <h2 className="text-3xl sm:text-4xl font-bold text-white tracking-tighter mb-3">
            Três passos. Três minutos.
          </h2>
          <p className="text-white/55 text-base leading-relaxed max-w-lg">
            Sem planilha pra preencher. Sem consultor pra contratar. Sem precisar saber contabilidade.
          </p>
        </InView>

        <div className="relative">
          {/* Connector line desktop only */}
          <div className="hidden md:block absolute top-10 left-[calc(16.5%+1px)] right-[calc(16.5%+1px)] h-px bg-white/10 z-0" />
          <motion.div
            className="grid grid-cols-1 md:grid-cols-3 gap-6 relative z-10"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true, margin: '-60px' }}
            variants={staggerContainer(0.12)}
          >
            {STEPS_FLOW.map(s => (
              <motion.div
                key={s.n}
                variants={fadeUpSpring}
                className="card-navy rounded-2xl p-7"
                whileHover={{ y: -2 }}
                transition={{ duration: 0.2 }}
              >
                <div className="flex items-center gap-3 mb-5">
                  <div className="w-10 h-10 rounded-xl bg-navy-950 flex items-center justify-center">
                    <span className="text-gold-400 font-bold text-sm font-mono tracking-tight">{s.n}</span>
                  </div>
                </div>
                <h3 className="text-base font-bold text-white mb-2.5 leading-snug">{s.title}</h3>
                <p className="text-sm text-white/55 leading-relaxed">{s.desc}</p>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </div>
    </section>
    ```

    **Features section (LP-07):**
    ```jsx
    <section className="py-24 sm:py-32">
      <div className="landing-container">
        <InView className="mb-14">
          <div className="flex items-center gap-3 mb-4">
            <span className="w-8 h-px bg-gold-500/60" />
            <span className="text-xs font-bold text-gold-400 uppercase tracking-widest">
              O QUE VOCÊ DESCOBRE
            </span>
          </div>
          <h2 className="text-3xl sm:text-4xl font-bold text-white tracking-tighter mb-3">
            Um relatório que seu contador respeitaria.
          </h2>
          <p className="text-white/55 text-base leading-relaxed">
            Mas escrito pra você ler num café, sem precisar de tradução.
          </p>
        </InView>

        <motion.div
          className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, margin: '-60px' }}
          variants={staggerContainer(0.06)}
        >
          {FEATURES.map(f => (
            <motion.div
              key={f.title}
              variants={fadeUpSpring}
              className="card-navy group p-6 rounded-2xl cursor-default"
              whileHover={{ y: -2 }}
              transition={{ duration: 0.18 }}
            >
              <div className={`w-10 h-10 rounded-xl flex items-center justify-center mb-4 ${f.accent}`}>
                <Icon d={f.d} size={18} />
              </div>
              <h3 className="text-[15px] font-bold text-white mb-2 leading-snug">{f.title}</h3>
              <p className="text-sm text-white/55 leading-relaxed">{f.desc}</p>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
    ```

    After Features, leave a stub:
    ```jsx
    {/* TODO Plan 03 Task 2: FAQ, CTA Final, Footer */}
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/mateu/Desktop/mvp cont/frontend" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -n "POR QUE DONO DE NEGÓCIO" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "id=\"como-funciona\"" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "FEATURES.map" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "STEPS_FLOW.map" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "PAIN_POINTS.map" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "money-" frontend/src/components/Landing.jsx` returns 0 lines
  </acceptance_criteria>
  <done>Pain Points, Como Funciona, and Features sections written with navy/gold palette and scroll-triggered stagger animations. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write FAQ, CTA Final, Footer sections — complete the Landing page</name>
  <files>frontend/src/components/Landing.jsx</files>
  <read_first>
    - frontend/src/components/Landing.jsx — find the `{/* TODO Plan 03 Task 2: FAQ, CTA Final, Footer */}` stub; confirm FaqItem component is present and correct
    - .planning/phases/02-landing-page/02-UI-SPEC.md — Component Inventory: FAQ, Footer specs; Copywriting Contract for footer tagline
    - .planning/phases/02-landing-page/02-RESEARCH.md — Pattern 6 (FAQ accordion), Don't Hand-Roll table (AnimatePresence)
  </read_first>
  <action>
    Replace the `{/* TODO Plan 03 Task 2: FAQ, CTA Final, Footer */}` stub with the following sections. After this task the file is complete.

    **FAQ section (LP-08):**
    ```jsx
    <section className="bg-navy-900 border-t border-white/8 py-24 sm:py-32">
      <div className="landing-container-narrow">
        <InView className="mb-10 text-center">
          <div className="flex items-center justify-center gap-3 mb-4">
            <span className="w-8 h-px bg-gold-500/60" />
            <span className="text-xs font-bold text-gold-400 uppercase tracking-widest">
              PERGUNTAS FREQUENTES
            </span>
            <span className="w-8 h-px bg-gold-500/60" />
          </div>
          <h2 className="text-3xl sm:text-4xl font-bold text-white tracking-tighter">
            O que você quer saber antes de começar
          </h2>
        </InView>

        <div className="space-y-2">
          {FAQS.map((item, i) => (
            <FaqItem key={item.q} q={item.q} a={item.a} i={i} />
          ))}
        </div>
      </div>
    </section>
    ```

    **CTA Final section (LP-03 reinforcement):**
    ```jsx
    <section className="relative overflow-hidden bg-navy-950 py-24 sm:py-32">
      {/* Reuse a centered glow orb for visual interest */}
      <div
        className="pointer-events-none absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full z-0 animate-[glow-pulse_12s_ease-in-out_infinite]"
        style={{ background: 'radial-gradient(circle, rgba(245,158,11,0.08) 0%, transparent 70%)' }}
      />
      <div className="landing-container-narrow relative z-10 text-center">
        <InView>
          <div className="flex items-center justify-center gap-3 mb-6">
            <span className="w-8 h-px bg-gold-500/40" />
            <span className="text-xs font-bold text-gold-400/70 uppercase tracking-widest">
              Comece agora
            </span>
            <span className="w-8 h-px bg-gold-500/40" />
          </div>
          <h2 className="text-3xl sm:text-[2.8rem] font-bold text-white tracking-tightest mb-5 leading-tight">
            Cinco minutos agora podem te
            <br />
            poupar meses no escuro.
          </h2>
          <p className="text-white/50 text-lg leading-relaxed mb-10">
            Crie sua conta, responda 7 perguntas e receba seu diagnóstico completo.
            Grátis para começar.
          </p>
          <motion.button
            onClick={onEnter}
            className="btn-gold inline-flex items-center gap-2"
            whileHover={{ scale: 1.03 }}
            whileTap={{ scale: 0.97 }}
            transition={{ duration: 0.15 }}
          >
            Começar diagnóstico — grátis
            <ArrowRight size={15} />
          </motion.button>
          <p className="text-xs text-white/25 mt-6">
            Diagnóstico grátis. Plano Pro para quem quer mais.
          </p>
        </InView>
      </div>
    </section>
    ```

    **Footer (LP-09):**
    ```jsx
    <footer className="bg-navy-950 border-t border-white/8 py-12">
      <div className="landing-container">
        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8 pb-8 border-b border-white/8">
          {/* Logo + tagline */}
          <div>
            <Logo dark />
            <p className="text-sm text-white/40 mt-3 leading-relaxed">
              Clareza financeira para quem toca o negócio.
            </p>
          </div>
          {/* Links */}
          <div>
            <p className="text-xs font-bold text-white/30 uppercase tracking-widest mb-3">Produto</p>
            <nav className="space-y-2">
              <a href="#como-funciona" className="block text-sm text-white/50 hover:text-white/80 transition-colors">Como funciona</a>
              <button onClick={onEnter} className="block text-sm text-white/50 hover:text-white/80 transition-colors text-left">Fazer diagnóstico</button>
            </nav>
          </div>
          {/* Legal */}
          <div>
            <p className="text-xs font-bold text-white/30 uppercase tracking-widest mb-3">Contato</p>
            <a href="mailto:finchecks@gmail.com"
              className="text-sm text-white/50 hover:text-white/80 underline underline-offset-2 transition-colors">
              finchecks@gmail.com
            </a>
          </div>
        </div>
        <div className="pt-6 flex flex-col sm:flex-row items-center justify-between gap-3">
          <p className="text-xs text-white/25">Feito pra empresas brasileiras · Dados salvos com segurança</p>
          <p className="text-xs text-white/20">© {new Date().getFullYear()} FinCheck</p>
        </div>
      </div>
    </footer>
    ```

    After Footer, the JSX of the main div closes:
    ```jsx
      {showUpgrade && <UpgradeModal onClose={() => setShowUpgrade(false)} />}
    </div>  {/* closes landing-root */}
    ```
    Then close the function and ensure NO `{/* TODO */}` stubs remain.

    **Final check after writing:** Scan the entire file for any remaining `money-*` color references (RESEARCH.md Pitfall 3). Replace any found with gold-* equivalents per the mapping:
    - `money-50` → `gold-50`, `money-100` → `gold-100`, `money-200` → `gold-200`
    - `money-400` → `gold-400`, `money-500` → `gold-500`, `money-600` → `gold-600`
    - `money-700` → `gold-700`, `money-800` → `gold-800`
    - `ink-*` color classes → replace with `white/*` (on dark bg) or `navy-*` as appropriate
    - Exception: `FEATURES` array `accent` strings like `bg-amber-50` or `bg-violet-50` — these use standard Tailwind tokens (not money-*) so leave them

    Run build to confirm no errors.
  </action>
  <verify>
    <automated>cd "C:/Users/mateu/Desktop/mvp cont/frontend" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -n "money-" frontend/src/components/Landing.jsx` returns 0 lines
    - `grep -n "FAQS.map" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "finchecks@gmail.com" frontend/src/components/Landing.jsx` returns 1 line (footer)
    - `grep -n "Clareza financeira para quem toca o negócio" frontend/src/components/Landing.jsx` returns 1 line
    - `grep -n "TODO" frontend/src/components/Landing.jsx` returns 0 lines (no stubs remain)
    - `grep -n "AnimatePresence" frontend/src/components/Landing.jsx` returns at least 2 lines (FAQ accordion)
    - File line count is between 600 and 1000 lines (`wc -l` or equivalent)
    - No TypeScript/Vite errors in build output
  </acceptance_criteria>
  <done>FAQ with animated accordion, CTA Final section, and Footer written. No money-* or TODO stubs remain. Full Landing.jsx complete. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete landing page redesign — all 9 LP requirements (LP-01 through LP-09) implemented in navy/gold palette:
    - LP-01: Hero animated background (2 glow orbs with glow-pulse keyframe)
    - LP-02: Headline stagger animation on load
    - LP-03: CTA btn-gold with hover/click feedback
    - LP-04: Sticky liquid-glass navbar
    - LP-05: Pain points section with glass-card items
    - LP-06: Como Funciona 3-step section with scroll entry
    - LP-07: Features 6-card grid with scroll stagger
    - LP-08: FAQ accordion with AnimatePresence
    - LP-09: Footer with tagline and email
  </what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` and open http://localhost:5173
    2. Hard refresh (Ctrl+Shift+R) — confirm headline animates line by line on load
    3. Check navbar is sticky and glass effect visible against hero on scroll
    4. Verify hero: two gold glow orbs animate slowly on dark navy background
    5. Hover CTA "Fazer meu diagnóstico" — confirm gold color + scale animation
    6. Click CTA — confirm scale-down on click
    7. Scroll to Pain section — confirm 6 items stagger-animate into view
    8. Scroll to Como Funciona — confirm 3 cards stagger-animate
    9. Scroll to Features — confirm 6 cards stagger-animate
    10. Click any FAQ item — confirm accordion opens/closes with smooth height animation
    11. Scroll to footer — confirm Logo, tagline "Clareza financeira para quem toca o negócio.", email link
    12. On desktop: scroll hero slowly — confirm ReportCard sub-sections reveal progressively
    13. Run `grep -rn "money-" frontend/src/components/Landing.jsx` — must return 0 lines
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which items failed</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
```bash
cd "C:/Users/mateu/Desktop/mvp cont/frontend"
npm run build
```
All 9 LP requirements verified visually. Build passes. No money-* classes remain.
</verification>

<success_criteria>
- All LP-05 through LP-09 requirements delivered
- Complete Landing.jsx with no TODO stubs
- No money-* color classes anywhere in the file
- FAQ AnimatePresence accordion functions correctly
- Footer renders with correct copy per UI-SPEC Copywriting Contract
- npm run build exits 0
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-03-SUMMARY.md` following the summary template.
</output>
